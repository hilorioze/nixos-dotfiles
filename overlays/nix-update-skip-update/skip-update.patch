diff --git a/nix_update/__init__.py b/nix_update/__init__.py
index cf8770d..60d010b 100644
--- a/nix_update/__init__.py
+++ b/nix_update/__init__.py
@@ -10,6 +10,7 @@ from pathlib import Path
 from typing import NoReturn
 
 from . import utils
+from .errors import SkipUpdateError
 from .eval import CargoLockInSource, Package, eval_attr
 from .options import Options
 from .update import update
@@ -472,7 +473,11 @@ def main(args: list[str] = sys.argv[1:]) -> None:
     if options.commit or options.review:
         git_dir = validate_git_dir(options.import_path)
 
-    package = update(options)
+    try:
+        package = update(options)
+    except SkipUpdateError as e:
+        info(str(e))
+        return
 
     print_maintainers(package)
     run_nix_commands(options)
diff --git a/nix_update/errors.py b/nix_update/errors.py
index 97b4503..013567d 100644
--- a/nix_update/errors.py
+++ b/nix_update/errors.py
@@ -11,3 +11,7 @@ class VersionError(UpdateError):
 
 class AttributePathError(UpdateError):
     pass
+
+
+class SkipUpdateError(Exception):
+    pass
diff --git a/nix_update/eval.nix b/nix_update/eval.nix
index 2b048c5..7261308 100644
--- a/nix_update/eval.nix
+++ b/nix_update/eval.nix
@@ -110,49 +110,52 @@ let
     else
       null;
 in
-{
-  name = pkg.name;
-  pname = pkg.pname or (builtins.parseDrvName pkg.name).name;
-  old_version = pkg.version or (builtins.parseDrvName pkg.name).version;
-  inherit raw_version_position;
-  filename = position.file;
-  line = position.line;
-  urls = pkg.src.urls or null;
-  url = pkg.src.url or null;
-  rev = pkg.src.rev or null;
-  tag = pkg.src.tag or null;
-  hash = pkg.src.outputHash or null;
-  fod_subpackage = pkg.outputHash or null;
-  go_modules = pkg.goModules.outputHash or null;
-  go_modules_old = pkg.go-modules.outputHash or null;
-  cargo_deps = pkg.cargoDeps.outputHash or null;
-  cargo_vendor_deps = pkg.cargoDeps.vendorStaging.outputHash or null;
-  raw_cargo_lock =
-    if pkg ? cargoDeps.lockFile then
-      let
-        inherit (pkg.cargoDeps) lockFile;
-        res = builtins.tryEval (sanitizePosition {
-          file = toString lockFile;
-        });
-      in
-      if res.success then res.value.file else false
-    else
-      null;
-  composer_deps = pkg.composerVendor.outputHash or null;
-  composer_deps_old = pkg.composerRepository.outputHash or null;
-  custom_deps = customHashes;
-  npm_deps = pkg.npmDeps.outputHash or null;
-  pnpm_deps = pkg.pnpmDeps.outputHash or null;
-  yarn_deps = pkg.yarnOfflineCache.outputHash or null;
-  yarn_deps_old = pkg.offlineCache.outputHash or null;
-  maven_deps = pkg.fetchedMavenDeps.outputHash or null;
-  has_nuget_deps = pkg ? nugetDeps;
-  has_gradle_mitm_cache = pkg ? mitmCache;
-  mix_deps = pkg.mixFodDeps.outputHash or null;
-  zig_deps = pkg.zigDeps.outputHash or null;
-  tests = builtins.attrNames (pkg.passthru.tests or { });
-  inherit has_update_script;
-  src_homepage = pkg.src.meta.homepage or null;
-  changelog = pkg.meta.changelog or null;
-  maintainers = pkg.meta.maintainers or null;
-}
+if pkg.passthru.skipUpdate or false then
+  { skip_update = true; }
+else
+  {
+    name = pkg.name;
+    pname = pkg.pname or (builtins.parseDrvName pkg.name).name;
+    old_version = pkg.version or (builtins.parseDrvName pkg.name).version;
+    inherit raw_version_position;
+    filename = position.file;
+    line = position.line;
+    urls = pkg.src.urls or null;
+    url = pkg.src.url or null;
+    rev = pkg.src.rev or null;
+    tag = pkg.src.tag or null;
+    hash = pkg.src.outputHash or null;
+    fod_subpackage = pkg.outputHash or null;
+    go_modules = pkg.goModules.outputHash or null;
+    go_modules_old = pkg.go-modules.outputHash or null;
+    cargo_deps = pkg.cargoDeps.outputHash or null;
+    cargo_vendor_deps = pkg.cargoDeps.vendorStaging.outputHash or null;
+    raw_cargo_lock =
+      if pkg ? cargoDeps.lockFile then
+        let
+          inherit (pkg.cargoDeps) lockFile;
+          res = builtins.tryEval (sanitizePosition {
+            file = toString lockFile;
+          });
+        in
+        if res.success then res.value.file else false
+      else
+        null;
+    composer_deps = pkg.composerVendor.outputHash or null;
+    composer_deps_old = pkg.composerRepository.outputHash or null;
+    custom_deps = customHashes;
+    npm_deps = pkg.npmDeps.outputHash or null;
+    pnpm_deps = pkg.pnpmDeps.outputHash or null;
+    yarn_deps = pkg.yarnOfflineCache.outputHash or null;
+    yarn_deps_old = pkg.offlineCache.outputHash or null;
+    maven_deps = pkg.fetchedMavenDeps.outputHash or null;
+    has_nuget_deps = pkg ? nugetDeps;
+    has_gradle_mitm_cache = pkg ? mitmCache;
+    mix_deps = pkg.mixFodDeps.outputHash or null;
+    zig_deps = pkg.zigDeps.outputHash or null;
+    tests = builtins.attrNames (pkg.passthru.tests or { });
+    inherit has_update_script;
+    src_homepage = pkg.src.meta.homepage or null;
+    changelog = pkg.meta.changelog or null;
+    maintainers = pkg.meta.maintainers or null;
+  }
diff --git a/nix_update/eval.py b/nix_update/eval.py
index 737bf63..781de38 100644
--- a/nix_update/eval.py
+++ b/nix_update/eval.py
@@ -7,7 +7,7 @@ from pathlib import Path
 from typing import TYPE_CHECKING, Any, Literal
 from urllib.parse import ParseResult, urlparse
 
-from .errors import UpdateError
+from .errors import SkipUpdateError, UpdateError
 from .utils import run
 from .version.version import Version, VersionPreference
 
@@ -152,6 +152,9 @@ def eval_attr(opts: Options) -> Package:
 
     res = run(cmd)
     out = json.loads(res.stdout)
+    if out.get("skip_update", False):
+        msg = f"Skipping {opts.attribute}: passthru.skipUpdate is set"
+        raise SkipUpdateError(msg)
     if opts.override_filename is not None:
         out["filename"] = opts.override_filename
     if opts.url is not None:
diff --git a/nix_update/update.py b/nix_update/update.py
index da065c3..a42fa30 100644
--- a/nix_update/update.py
+++ b/nix_update/update.py
@@ -7,7 +7,7 @@ from typing import TYPE_CHECKING
 
 from .dependency_hashes import update_dependency_hashes, update_src_hash
 from .diff_urls import generate_diff_url
-from .errors import UpdateError
+from .errors import SkipUpdateError, UpdateError
 from .eval import Package, eval_attr
 from .git import old_version_from_git
 from .utils import info, run
@@ -229,7 +229,10 @@ def update(opts: Options) -> Package:
             subpackage_opts.subpackages = None
             # Do not update the version number since that's already been done
             subpackage_opts.version_preference = VersionPreference.SKIP
-            update(subpackage_opts)
+            try:
+                update(subpackage_opts)
+            except SkipUpdateError as e:
+                info(str(e))
 
     update_dependency_hashes(opts, package, update_hash=update_hash)
 
